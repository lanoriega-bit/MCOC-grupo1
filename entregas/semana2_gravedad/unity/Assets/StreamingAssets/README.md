The Unity viewer reads the source JSON from:

../../results/edificio1_unity.json

relative to Application.dataPath (`Assets`). The gravity result JSON is not copied
or modified here, so the Unity project always consumes the Semana 2 E1 result file.
